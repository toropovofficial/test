import Vue from 'vue'
import Vuex from 'vuex'
import GeneralTaskList from './modules/generalStore'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    GeneralTaskList
  }
})
